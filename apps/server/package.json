{
  "name": "@dogebot/server",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json && node -e \"const fs=require('fs'); const path=require('path'); const copyDir=(src,dst,filter)=>{ if(!fs.existsSync(src)) return; fs.mkdirSync(dst,{recursive:true}); for(const name of fs.readdirSync(src)){ const from=path.join(src,name); const to=path.join(dst,name); const stat=fs.statSync(from); if(stat.isDirectory()){ copyDir(from,to,filter); continue; } if(filter && !filter(name,from)) continue; fs.mkdirSync(path.dirname(to),{recursive:true}); fs.copyFileSync(from,to); } }; copyDir(path.join(process.cwd(),'scripts'), path.join(process.cwd(),'dist','scripts'), (name)=>name.endsWith('.py')); copyDir(path.join(process.cwd(),'assets'), path.join(process.cwd(),'dist','assets'));\"",
    "dev": "tsx watch src/index.ts",
    "start": "node dist/index.js",
    "add-user": "tsx scripts/add-user.ts",
    "test:sticker-hdr": "node --import tsx --test tests/style-sticker-hdr.test.ts"
  },
  "dependencies": {
    "@larksuiteoapi/node-sdk": "^1.66.1",
    "better-sqlite3": "^11.8.1",
    "colord": "^2.9.3",
    "cors": "^2.8.5",
    "express": "^4.21.2",
    "@syru/byted-sticker-generator": "1.0.0-beta.2"
  },
  "devDependencies": {
    "@types/better-sqlite3": "^7.6.13",
    "@types/cors": "^2.8.17",
    "@types/express": "^5.0.0",
    "@types/node": "^22.10.7",
    "tsx": "^4.19.2",
    "typescript": "^5.7.3"
  }
}
